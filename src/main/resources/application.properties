spring.sql.init.platform=postgres
# 1. PostgreSQL connection ? these 4 lines are mandatory
spring.datasource.url=jdbc:postgresql://db.jyzpcvosismwilptumpp.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=KHYA43bVfFyNhGWa

# optional but very useful during development
spring.datasource.driver-class-name=org.postgresql.Driver

# 2. Tell Spring Boot to run your schema.sql & data.sql files
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
# Options: none, validate, update, create, create-drop
spring.jpa.hibernate.ddl-auto=update
spring.docker.compose.enabled=false
spring.main.allow-bean-definition-overriding=true

#jwt

jwt.secret=94-hjkdjnrejfkrhrfrj@#239-93ym%781
jwt.expiration-ms=86400000

app.base-url=http://localhost:8080

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hanaabdelbari882@gmail.com
spring.mail.password=0000
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
#CartManagement package
#to avoid this nums hardcoded in checkoutService in cart package
# Tax rate applied to order subtotal
# Set to 0.00 for MVP ? change to 0.15 for Saudi VAT, 0.14 for Egypt VAT
app.tax.rate=0.00

# Flat shipping cost applied to every order regardless of quantity or weight
# Change to 0.00 to offer free shipping
app.shipping.flat-rate=25.00